{{#each artists}}
  <div>
    <h2>{{name}}</h2>
    {{#if images.length}}
      <img src="{{images.0.url}}" alt="Artist Image" />
    {{else}}
      <p>No image available</p>
    {{/if}}
    <a href="/albums/{{id}}">View Albums</a>
  </div>
{{/each}}