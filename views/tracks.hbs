{{#each tracks}}
  <div>
    <h2>{{name}}</h2>
    <audio controls>
      <source src="{{preview_url}}" type="audio/mpeg" />
      Your browser does not support the audio element.
    </audio>
  </div>
{{/each}}